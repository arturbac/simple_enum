name: C++ CI

on:
  push:
    paths-ignore:
      - 'README.md'
  pull_request:
    paths-ignore:
      - 'README.md'

jobs:
  build-and-test-gcc:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: sudo apt-get update && sudo apt-get install -y cmake ninja-build gcc g++ clang libfmt-dev

    - name: Configure CMake with gcc
      run: cmake -B ${{github.workspace}}/build/gcc -DCMAKE_BUILD_TYPE=Release -G "Ninja" -DCMAKE_CXX_COMPILER=g++
    - name: Build with gcc
      run: cmake --build ${{github.workspace}}/build/gcc
    - name: Test with gcc
      working-directory: ${{github.workspace}}/build/gcc
      run: ctest
      
  build-and-test-msvc:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - name: Configure CMake with MSVC
      run: cmake -B ${{github.workspace}}/build/msvc -DCMAKE_BUILD_TYPE=Debug
    - name: Build with MSVC
      run: cmake --build ${{github.workspace}}/build/msvc
    - name: Test with MSVC
      working-directory: ${{github.workspace}}/build/msvc
      run: ctest -C Debug
      
    # - name: Configure CMake with clang
    #   run: cmake -B ${{github.workspace}}/build/clang -DCMAKE_BUILD_TYPE=Release -G "Ninja" -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_C_COMPILER=clang
    # - name: Build with clang
    #   run: cmake --build ${{github.workspace}}/build/clang
    # - name: Test with clang
    #   working-directory: ${{github.workspace}}/build/clang
    #   run: ctest
